import { TextoEncabezadoPipe } from './texto-encabezado.pipe';

describe('TextoEncabezadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoEncabezadoPipe();
    expect(pipe).toBeTruthy();
  });
});
