import { TextoEncabezadoDirective } from './texto-encabezado.directive';

describe('TextoEncabezadoDirective', () => {
  it('should create an instance', () => {
    const directive = new TextoEncabezadoDirective();
    expect(directive).toBeTruthy();
  });
});
